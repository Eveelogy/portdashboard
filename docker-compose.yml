services:
  app:
    build: 
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "9595:9595"
    volumes:
      - ./backend/ports.db:/app/ports.db
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - PYTHONUNBUFFERED=1
